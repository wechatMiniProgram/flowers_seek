<!--index.wxml-->
 <view wx:for="{{flowerData}}" wx:key="name" class="item">
    <image src="{{item.url}}"></image>
    <view class="seek_button">
        <button bindtap="seekMeaning" id="{{item.name}}">{{item.name}}</button>
    </view>
 </view>
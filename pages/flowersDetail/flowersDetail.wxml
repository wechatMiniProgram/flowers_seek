<!--index.wxml-->
<view>

 <view wx:for="{{flowerData}}" wx:key="name" class="item">
 <image src="{{item.url}}"></image>
         <label>{{item.name}}</label>
         <button>查看详情</button>
 </view>

</view>